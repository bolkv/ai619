"""AMOS CT multi-organ segmentation (MaskSAM-trained nnU-Net v2).

Vendors the nnUNet source under ``vendor/`` so it does not need to be
installed system-wide; ``infer.py`` adds it to ``sys.path`` on import.
"""
